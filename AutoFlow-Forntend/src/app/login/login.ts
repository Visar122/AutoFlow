import { Component } from '@angular/core';
import { SHARED } from '../shared';

@Component({
  selector: 'app-login',
  imports: [SHARED],
  templateUrl: './login.html',
  styleUrl: './login.css',
})
export class Login {}
